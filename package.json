{
  "scripts": {
    "format": "npx prettier --plugin prettier-plugin-sh --write ."
  },
  "dependencies": {
    "prettier": "^3.8.0",
    "prettier-plugin-sh": "^0.18.0"
  },
  "prettier": {
    "tabWidth": 2,
    "useTabs": false
  }
}
