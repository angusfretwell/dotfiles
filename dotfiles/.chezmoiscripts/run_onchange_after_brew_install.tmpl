#!/usr/bin/env bash
# MacOS build version: {{ output "sw_vers" "--buildVersion" }}
# Brewfile hash: {{ include (joinPath .chezmoi.workingTree "Brewfile") | sha256sum }}

brew update
brew bundle -gv --jobs 4
brew bundle cleanup -g --force
